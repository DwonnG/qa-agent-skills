# Skills index

See the [repository README](../README.md) for the full catalog and install steps.

Before use, configure project key (`PROJ`), Confluence space, Jira custom field IDs, Jenkins job paths, and GitHub org in each skill’s **Project Configuration** section.
